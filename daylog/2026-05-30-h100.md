# 2026-05-30 (H100)

E1–E7 (`2026-05-29.md`) were on B200 183GB; today is H100 80GB (matches branch
intent + BENCHMARKS.md). Treat B200 as a separate reference — today re-baselines.

## Method
- Daylog stays in the E1–E7 style; add + commit + push after every experiment.
- Empirics with command + conditions, not claims. Measure what would be a guess.
- Understand before fixing.

## Setup
- Served `Qwen/Qwen3.6-35B-A3B-FP8` (as `Qwen/Qwen3.6-35B-A3B`). `/v1/models` → 200.
- Flags: `--max-model-len 32768 --gpu-memory-utilization 0.90 --enable-prefix-caching --enable-chunked-prefill --language-model-only --reasoning-parser qwen3 --default-chat-template-kwargs '{"enable_thinking":false}' --max-num-batched-tokens 8192`.
- Box: H100 80GB, vLLM 0.22.0, torch 2.11.0+cu130, CUDA 13.0, py 3.12. Installed wheel; launch from neutral cwd (clone dir shadows `vllm._C`).
- Cold start: weights 41s (cached), torch.compile + warmup, DeepGEMM 1253 kernels (~5 min). DeepGEMM = per-shape JIT FP8 GEMM (`sm90_fp8_gemm_1d2d`), cached .cu+.cubin under `~/.cache/vllm/deep_gemm/`.
- Attn block = **1056** (`interface.py:649` "attention page size >= mamba page size"; `:673` mamba padded 0.76% to match). Same as B200 → arch-derived, not GPU.
- KV: 32.52 GiB → 1.39M tok; 42.5× concurrency @32k. (B200: 122 GiB, 5.23M, 159×.)

## E8 — H100 baseline TTFT sweep 3k–5k, C=1, no cache
`ttft_sweep.py --lengths 3000,3168,3500,4000,4096,4224,4500,4752,5000 --concurrency 1 --n 12 --warm 4`. Same method as E1 (fresh random ids, 5 out-tok, stream; 8 samples/len). Scope = the 3k–5k serving path.

| in | p50 | p90 | min | split |
|---:|---:|---:|---:|---|
| 3000 | 173.0 | 175.6 | 169.7 | 2112+888 |
| 3168 | 93.9 | 95.2 | 92.9 | 3×1056 |
| 3500 | 114.5 | 116.9 | 112.6 | 3168+332 |
| 4000 | 171.2 | 188.0 | 168.8 | 3168+832 |
| 4096 | 173.0 | 175.3 | 170.7 | 3168+928 |
| 4224 | 99.5 | 105.3 | 96.5 | 4×1056 |
| 4500 | 117.5 | 124.9 | 114.6 | 4224+276 |
| 4752 | 179.4 | 202.5 | 166.0 | 4224+528 |
| 5000 | 174.8 | 181.3 | 169.6 | 4224+776 |

Sawtooth reproduced. Fast = only the exact 1056-multiples (3168, 4224, ~94–100); 4096=173.0 ≈ 1.74× 4224=99.5 (chunk-split penalty). Non-aligned splits into two tiers by remainder: ≤332 → ~115; ≥528 → ~171–179 (threshold 332–528, ≈½ block). p90 tight → real.

## Knobs (from `--help`, to test next)
- `--block-size N` (CacheConfig, default None→derived 1056) — attention KV block.
- `--mamba-block-size N` (multiple of 8, needs prefix caching) — mamba cache block.

## TODO
- [ ] Force unified page ≥8192 so prompts ≤8192 prefill in one chunk; re-run E8 grid before/after.
- [ ] Explain the two-tier remainder threshold (332 vs 528).
- [ ] E6 roofline for chunked gated-deltanet (real FLOPs, not O(L)).
- [ ] C=2 of the band.
